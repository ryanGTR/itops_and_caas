# --- Secrets / credentials(務必別進版控,呼應 TASK-03 機敏掃描) ---
*.pem
*.key
*.p12
*.pfx
.env
.env.*
!.env.example
secrets/
credentials/

# --- Python(scripts/ 與後續稽核工具) ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/

# --- 稽核報告產出(TASK-07,屬產出物不進版控) ---
audit-reports/
*.audit.csv
*.audit.md

# --- Terraform / OpenTofu(IaC 產出,不進版控;tfstate 可能含機敏) ---
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log
# tfvars 可能含機敏值;範例檔保留
*.tfvars
*.tfvars.json
!*.tfvars.example
# 註:.terraform.lock.hcl 刻意「不」忽略——應納管以鎖定 provider 版本

# --- OS / 編輯器 ---
.DS_Store
Thumbs.db
*.swp
*~
.idea/
.vscode/

# 整合交接產物(執行期生成,不版控)
integration/inbox/
